[tox]
envlist = {env:PYTHON_VERSIONS}


[testenv]
passenv = *
usedevelop = True
deps =
    pytest
    pytest-cov
commands =
    pytest {posargs}
setenv =
    COVERAGE_FILE = artifacts/.coverage.{envname}
